%!PS
%%Title: Letter - Asymmetric Circle Grid, 8x5, 2cm Circles
%%Pages: 1
/pagewidth 8.5 72 mul def
/pageheight 10 72 mul def 

% ----- Define centimeter
/cm {28.346 mul} def
% ----- square width
/w {6 cm} def
/d {3 cm} def
/r {1 cm} def
% ----- Define procedure for drawing a box
/circle {r 0 360 arc closepath} def

% take in account the size of a square
/owidth pagewidth def
/oheight pageheight def
/pagewidth pagewidth w div floor w mul def
/pageheight pageheight w div floor w mul def
/offw owidth pagewidth sub 2 div d add def
/offh oheight pageheight sub 2 div  def

% draw all the boxes it can across a single row
/rowboxes {w pagewidth {y d add circle fill} for} def

% increments the y variable and draws all the rows
offh w pageheight { /y exch def offw rowboxes } for
offh d add w pageheight { /y exch def offw d add rowboxes } for

/Times-Roman findfont
7 scalefont setfont newpath 1.5 cm 1.5 cm moveto
(BoofCV : Asymmetric Circle Grid, 8x5, 2cm diameter, 6cm distance) show

showpage

%%EOF
